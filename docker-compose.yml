version: '3'

services:
  face-detection:
    build: .
    devices:
      - /dev/video0
    environment:
      DISPLAY: $DISPLAY
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ~/.Xauthority:/root/.Xauthority
    network_mode: 'host'
